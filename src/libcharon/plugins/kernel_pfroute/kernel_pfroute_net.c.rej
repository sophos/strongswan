diff a/src/external/thirdparty/strongswan/src/libcharon/plugins/kernel_pfroute/kernel_pfroute_net.c b/src/external/thirdparty/strongswan/src/libcharon/plugins/kernel_pfroute/kernel_pfroute_net.c	(rejected hunks)
@@ -786,12 +848,26 @@ static void repopulate_iface(private_kernel_pfroute_net_t *this,
 							 iface_entry_t *iface)
 {
 	struct ifaddrs *ifap, *ifa;
-	addr_entry_t *addr;
+	addr_entry_t *addr, *vaddr = NULL;
 
 	while (iface->addrs->remove_last(iface->addrs, (void**)&addr) == SUCCESS)
 	{
 		addr_map_entry_remove(addr, iface, this);
-		addr_entry_destroy(addr);
+		/* Check for the virtual IP address. If we see it, we will save it so we
+		   can set it later when the address list is rebuilt. Note that we only
+		   save one, so the last virtual address is the one we will use. */
+		if (addr->virtual)
+		{
+			if (vaddr)
+			{
+				addr_entry_destroy(vaddr);
+			}
+			vaddr = addr;
+		}
+		else
+		{
+			addr_entry_destroy(addr);
+		}
 	}
 
 	if (getifaddrs(&ifap) == 0)
@@ -807,6 +883,10 @@ static void repopulate_iface(private_kernel_pfroute_net_t *this,
 						INIT(addr,
 							.ip = host_create_from_sockaddr(ifa->ifa_addr),
 						);
+						if (vaddr && addr->ip->ip_equals(addr->ip, vaddr->ip))
+						{
+							addr->virtual = TRUE;
+						}
 						iface->addrs->insert_last(iface->addrs, addr);
 						addr_map_entry_add(this, addr, iface);
 						break;
@@ -817,6 +897,10 @@ static void repopulate_iface(private_kernel_pfroute_net_t *this,
 		}
 		freeifaddrs(ifap);
 	}
+	if (vaddr)
+	{
+		addr_entry_destroy(vaddr);
+	}
 }
 
 /**
