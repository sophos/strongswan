diff a/src/external/thirdparty/strongswan/src/libcharon/plugins/kernel_libipsec/kernel_libipsec_plugin.c b/src/external/thirdparty/strongswan/src/libcharon/plugins/kernel_libipsec/kernel_libipsec_plugin.c	(rejected hunks)
@@ -145,5 +145,6 @@ plugin_t *kernel_libipsec_plugin_create()
 	/* set TUN device as default to install VIPs */
 	lib->settings->set_str(lib->settings, "%s.install_virtual_ip_on",
 						   this->tun->get_name(this->tun), lib->ns);
+	this->tun->close_tun(this->tun);
 	return &this->public.plugin;
 }
